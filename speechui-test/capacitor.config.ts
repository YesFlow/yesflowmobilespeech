import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yesflow.mobilespeech',
  appName: 'speechui-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
